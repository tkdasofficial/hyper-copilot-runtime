name: Create Video
run-name: "Video - ${{ github.event.client_payload.title || github.event.client_payload.prompt || inputs.prompt || 'Manual Build' }}"

on:
  repository_dispatch:
    types: [create-video, create_video, long_form, long]
  workflow_dispatch:
    inputs:
      prompt:
        description: "Video prompt or topic"
        required: true
        default: "Cosmic journey through deep space"
      video_id:
        description: "Supabase Video ID (optional)"
        required: false
        default: ""

concurrency:
  group: ${{ github.workflow }}-${{ github.event.client_payload.video_id || inputs.video_id || github.run_id }}
  cancel-in-progress: false

jobs:
  create-video:
    name: create-video
    runs-on: ubuntu-latest
    timeout-minutes: 60

    env:
      SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
      SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
      CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
      CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
      PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}
      PIXABAY_API_KEY: ${{ secrets.PIXABAY_API_KEY }}
      PIXAZO_API_KEY: ${{ secrets.PIXAZO_API_KEY }}
      NVIDIA_API_KEY: ${{ secrets.NVIDIA_API_KEY }}
      GOOGLE_CLIENT_ID: ${{ secrets.GOOGLE_CLIENT_ID }}
      GOOGLE_CLIENT_SECRET: ${{ secrets.GOOGLE_CLIENT_SECRET }}
      GOOGLE_REFRESH_TOKEN: ${{ secrets.GOOGLE_REFRESH_TOKEN }}
      GDRIVE_MAIN_FOLDER_ID: ${{ secrets.GDRIVE_MAIN_FOLDER_ID }}
      GDRIVE_CLIENT_EMAIL: ${{ secrets.GDRIVE_CLIENT_EMAIL }}
      GDRIVE_PRIVATE_KEY: ${{ secrets.GDRIVE_PRIVATE_KEY }}
      GDRIVE_DELEGATED_USER: ${{ secrets.GDRIVE_DELEGATED_USER }}
      VIDEO_ID: ${{ github.event.client_payload.video_id || inputs.video_id || github.run_id }}
      USER_ID: ${{ github.event.client_payload.user_id || 'github-actions' }}
      PROMPT: ${{ github.event.client_payload.prompt || inputs.prompt }}
      NEGATIVE_PROMPT: ${{ github.event.client_payload.negative_prompt }}
      VOICE_GENDER: ${{ github.event.client_payload.voice_gender || 'male' }}
      VOICE_PERSONA: ${{ github.event.client_payload.voice_persona || github.event.client_payload.category || 'Documentary' }}
      VIDEO_STYLE: ${{ github.event.client_payload.visual_style || github.event.client_payload.image_style || 'Cinematic' }}
      ASPECT_RATIO: ${{ github.event.client_payload.aspect_ratio || '16:9' }}
      DURATION_SECONDS: ${{ github.event.client_payload.duration_seconds || '180' }}
      CAPTIONS: ${{ github.event.client_payload.captions || 'true' }}
      PIPELINE_MODE: "long"
      TARGET_RATIO: "16:9"
      RENDER_FPS: "30"

    steps:
      - name: "Step 1: Checkout Repository"
        uses: actions/checkout@v4

      - name: "Step 2: Install Dependencies & Compile"
        run: |
          sudo apt-get update -y
          sudo apt-get install -y \
            build-essential cmake pkg-config \
            libavcodec-dev libavformat-dev libavfilter-dev libswscale-dev libswresample-dev libavutil-dev \
            libfreetype-dev libharfbuzz-dev nlohmann-json3-dev libomp-dev \
            ffmpeg fonts-noto-cjk fonts-dejavu-core fonts-freefont-ttf imagemagick
          sudo apt-get install -y python3-pip python3-setuptools
          pip3 install --break-system-packages edge-tts || pip install edge-tts || true
          which edge-tts || true
          chmod +x editor/build.sh || true
          ./editor/build.sh || echo "C++ build warning; FFmpeg master pipeline active"

      - name: "Step 3: Import Stock Footages & Run Editor"
        run: |
          node editor/prepare-assets.js

      - name: "Step 4: MP4 Rendering"
        run: |
          node editor/export-video.js

      - name: "Step 5: Drive Upload & Supabase Sync"
        if: always()
        run: |
          node editor/post-process-sync.js

      - name: "Step 6: Cleanup Workspace"
        if: always()
        run: |
          rm -rf temp_* *.tmp assets/*.mp4 assets/*.mp3 || true
